package com.example.shoppingappfrontend.cart;

import androidx.lifecycle.ViewModel;
// PUBLIC_INTERFACE
/** CartViewModel handles the logic for shopping cart items and checkout. */
public class CartViewModel extends ViewModel {
    // TODO: Expose LiveData for cart items, call CartRepository for actions
}
